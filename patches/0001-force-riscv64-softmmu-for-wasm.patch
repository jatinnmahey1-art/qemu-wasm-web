diff --git a/configure b/configure
index 1a2b3c4..5f6e7d8 100755
--- a/configure
+++ b/configure
@@ -1,6 +1,16 @@
 #!/bin/sh
 # QEMU configure script
 
+# === EMSCRIPTEN: FORCE riscv64-softmmu TARGET ===
+if [ "$cpu" = "wasm32" ] || [ "$cpu" = "wasm64" ]; then
+  case " $target_list " in
+    *" riscv64-softmmu "* ) ;;
+    *) target_list="riscv64-softmmu $target_list" ;;
+  esac
+  echo "Emscripten: forcing riscv64-softmmu"
+fi
+# ================================================
+
 # Default values
 softmmu="yes"
 linux_user="no"
